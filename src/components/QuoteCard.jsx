const QuoteCard = function(){
    return(
    <div>
        <p> "Nothing fixes a thing so firmly in the memory as the wish to forget it."</p>
        <small>Rebecca</small>
        <p>This hit harder than I expected</p>
    </div>
    )
}
export default QuoteCard